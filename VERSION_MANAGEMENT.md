# バージョン管理とアップデート手順

## バージョンの更新方法

アプリに変更を加えた後、ユーザーに強制的に更新させるには：

1. `sw.js` ファイルを開く
2. `VERSION` 定数を更新する
   ```javascript
   const VERSION = 'v1.5.1'; // バージョンを上げる
   ```
3. ファイルを保存

## 自動更新の仕組み

### キャッシュ管理
- キャッシュ名: `math-game-v1.5.0` (バージョンが含まれる)
- 新しいバージョンがインストールされると、古いキャッシュは自動的に削除される
- ユーザーが次回アプリを開いたときに新しいバージョンが適用される

### 更新検出
- Service Workerは60秒ごとに更新をチェック
- 新しいバージョンが検出されると、自動的にダウンロードして適用
- ページが自動リロードされ、最新版が表示される

## テスト方法

1. ローカルサーバーでアプリを起動
2. ブラウザの開発者ツールを開く
3. Application > Service Workers で現在のバージョンを確認
4. コードを変更
5. `sw.js` のバージョンを上げる
6. ページをリロード
7. 新しいService Workerがインストールされることを確認
8. 古いキャッシュが削除されることを確認

## 注意事項

- **Network-first戦略**: 常にネットワークから最新版を取得し、失敗した場合のみキャッシュを使用
- **即座に有効化**: `skipWaiting()` と `clients.claim()` により、新しいSWが即座に有効になる
- **自動リロード**: 新しいバージョンが有効になると、ページが自動的にリロードされる
